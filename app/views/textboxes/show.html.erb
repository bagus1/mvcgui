<%#= @textbox.size %>

<br><%=text_field_tag(
	obj.tablename + "[" + displayable_attribute[:attribute].field + "]", 
	obj.read_attribute(displayable_attribute[:attribute].field), 
	size: displayable_attribute[:display_format].size 
	)%>